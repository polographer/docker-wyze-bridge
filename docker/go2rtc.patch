diff --git a/pkg/tutk/dtls/conn_dtls.go b/pkg/tutk/dtls/conn_dtls.go
index c1d5f6c..22f1044 100644
--- a/pkg/tutk/dtls/conn_dtls.go
+++ b/pkg/tutk/dtls/conn_dtls.go
@@ -111,10 +111,14 @@ type DTLSConn struct {
 	rxSeqStart uint16
 	rxSeqEnd   uint16
 	rxSeqInit  bool
-	cmdAck     func()
+	cmdAck        func()
+
+	// discoverTimeout controls how long discovery() waits for the
+	// camera to respond to UDP discovery packets.
+	discoverTimeout time.Duration
 }
 
-func DialDTLS(host string, port int, uid, authKey, enr string, verbose bool) (*DTLSConn, error) {
+func DialDTLS(host string, port int, uid, authKey, enr string, verbose bool, discoverTimeout time.Duration) (*DTLSConn, error) {
 	udp, err := net.ListenUDP("udp", nil)
 	if err != nil {
 		return nil, err
@@ -140,7 +144,8 @@ func DialDTLS(host string, port int, uid, authKey, enr string, verbose bool) (*D
 		ctx:        ctx,
 		cancel:     cancel,
 		rxSeqStart: 0xffff,
-		rxSeqEnd:   0xffff,
+		rxSeqEnd:     0xffff,
+		discoverTimeout: discoverTimeout,
 	}
 
 	if err = c.discovery(); err != nil {
@@ -503,7 +508,7 @@ func (c *DTLSConn) discovery() error {
 	pktCC51 := c.msgDiscoCC51(0, 0, false)
 
 	buf := make([]byte, 2048)
-	deadline := time.Now().Add(5 * time.Second)
+	deadline := time.Now().Add(c.discoverTimeout)
 
 	for time.Now().Before(deadline) {
 		c.conn.WriteToUDP(pktIOTC, c.addr)
diff --git a/pkg/wyze/client.go b/pkg/wyze/client.go
index 0fe878e..6e40a8f 100644
--- a/pkg/wyze/client.go
+++ b/pkg/wyze/client.go
@@ -69,7 +69,8 @@ type Client struct {
 
 	audioCodecID    byte
 	audioSampleRate uint32
-	audioChannels   uint8
+	audioChannels       uint8
+	timeout             time.Duration
 }
 
 type AuthResponse struct {
@@ -96,6 +97,15 @@ func Dial(rawURL string) (*Client, error) {
 		mac:     query.Get("mac"),
 		model:   query.Get("model"),
 		verbose: query.Get("verbose") == "true",
+
+	// Default to 5s discovery timeout; allow override via ?timeout=Xs
+	// query parameter on the wyze:// source URL.
+	c.timeout = 5 * time.Second
+	if td := query.Get("timeout"); td != "" {
+		if d, err := time.ParseDuration(td); err == nil {
+			c.timeout = d
+		}
+	}
 	}
 
 	c.authKey = string(dtls.CalculateAuthKey(c.enr, c.mac))
@@ -304,7 +314,7 @@ func (c *Client) connect() error {
 		host = host[:idx]
 	}
 
-	conn, err := dtls.DialDTLS(host, port, c.uid, c.authKey, c.enr, c.verbose)
+	conn, err := dtls.DialDTLS(host, port, c.uid, c.authKey, c.enr, c.verbose, c.timeout)
 	if err != nil {
 		return fmt.Errorf("wyze: connect failed: %w", err)
 	}
