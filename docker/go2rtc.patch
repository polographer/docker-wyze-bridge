diff -ruN a/pkg/tutk/dtls/conn_dtls.go b/pkg/tutk/dtls/conn_dtls.go
--- a/pkg/tutk/dtls/conn_dtls.go	2026-06-07 15:42:27.410682483 -0400
+++ b/pkg/tutk/dtls/conn_dtls.go	2026-06-07 15:42:27.446683922 -0400
@@ -112,9 +112,12 @@
 	rxSeqEnd   uint16
 	rxSeqInit  bool
 	cmdAck     func()
+	// discoverTimeout controls how long discovery() waits for the
+	// camera to respond to UDP discovery packets.
+	discoverTimeout   time.Duration
 }
 
-func DialDTLS(host string, port int, uid, authKey, enr string, verbose bool) (*DTLSConn, error) {
+func DialDTLS(host string, port int, uid, authKey, enr string, verbose bool, discoverTimeout time.Duration) (*DTLSConn, error) {
 	udp, err := net.ListenUDP("udp", nil)
 	if err != nil {
 		return nil, err
@@ -141,6 +144,7 @@
 		cancel:     cancel,
 		rxSeqStart: 0xffff,
 		rxSeqEnd:   0xffff,
+		discoverTimeout: discoverTimeout,
 	}
 
 	if err = c.discovery(); err != nil {
@@ -503,7 +507,7 @@
 	pktCC51 := c.msgDiscoCC51(0, 0, false)
 
 	buf := make([]byte, 2048)
-	deadline := time.Now().Add(5 * time.Second)
+	deadline := time.Now().Add(c.discoverTimeout)
 
 	for time.Now().Before(deadline) {
 		c.conn.WriteToUDP(pktIOTC, c.addr)
diff -ruN a/pkg/wyze/client.go b/pkg/wyze/client.go
--- a/pkg/wyze/client.go	2026-06-07 15:42:27.411438587 -0400
+++ b/pkg/wyze/client.go	2026-06-07 15:42:27.466599677 -0400
@@ -85,6 +85,12 @@
 
 	query := u.Query()
 
+	discoverTimeout := 5 * time.Second
+	if td := query.Get("timeout"); td != "" {
+		if d, err := time.ParseDuration(td); err == nil {
+			discoverTimeout = d
+		}
+	}
 	if query.Get("dtls") != "true" {
 		return nil, fmt.Errorf("wyze: only DTLS cameras are supported")
 	}
@@ -304,7 +310,7 @@
 		host = host[:idx]
 	}
 
-	conn, err := dtls.DialDTLS(host, port, c.uid, c.authKey, c.enr, c.verbose)
+	conn, err := dtls.DialDTLS(host, port, c.uid, c.authKey, c.enr, c.verbose, discoverTimeout)
 	if err != nil {
 		return fmt.Errorf("wyze: connect failed: %w", err)
 	}
